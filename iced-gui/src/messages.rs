use crate::state::{
    Alarm, AlarmOccurrence, AppPage, CloseTaskBehavior, ColorMode, DeviceSelect, DeviceType,
    LoginResponse, PendingDelete, UpdateResponse,
};
use crate::websocket::WsMessage;
use iced::{window, Color};
use iced_aw::date_picker::Date;
use iced_aw::time_picker::Time;

#[derive(Clone, Debug)]
pub enum Message {
    ServerAddressChanged(String),
    CameraError(String),
    FrameTick,
    ValidateSession,
    SessionInvalid,
    FetchUpdate,
    UpdateReceived(UpdateResponse),
    UpdateError(String),
    EmailChanged(String),
    PasswordChanged(String),
    Submit,
    LoginResult(Result<LoginResponse, String>),
    ClearError,
    ToggleQrScanner,
    QrTokenInputChanged(String),
    QrSubmit(String),
    QrLoginResult(Result<LoginResponse, String>),
    CloseQrScanner,
    StartScanner,
    QrScanned(Option<String>),
    QrFrameRefresh(Option<(u32, u32, Vec<u8>)>),
    NavigateTo(AppPage),
    GoToRegister,
    GoToLogin,
    GoToLogout,
    RegisterFirstNameChanged(String),
    RegisterLastNameChanged(String),
    RegisterEmailChanged(String),
    RegisterPasswordChanged(String),
    RegisterConfirmPasswordChanged(String),
    SubmitRegister,
    RegisterResult(Result<(), String>),
    ToggleClock24,
    ToggleSettings,
    SetNavBarTop(bool),
    SetPanelSize(u32),
    SetVolume(f32),
    SetDialogSize(u32),
    ToggleColors,
    SetColorMode(ColorMode),
    SetCardColorEven(String),
    SetCardColorOdd(String),
    SetCardColorInactive(String),
    SetCardColorBackground(String),
    SetDefaultCardColors,
    SetCurrentCardColor(String),
    SetColorHue(f32),
    SetColorSV(f32, f32),
    OpenColorPicker,
    SubmitColorPicker(Color),
    CancelColorPicker,
    SetCloseTaskBehavior(CloseTaskBehavior),
    SetSnoozePressMs(u32),
    SetNotificationsEnabled(bool),
    SetDesktopNotificationsEnabled(bool),
    WsConnect,
    WsDisconnect,
    WsMessageReceived(Result<WsMessage, String>),
    PlayAudio(String),
    StopAudio,
    PreviewTune(String),
    PlayPreviewAudio(String),
    StopPreviewTune,
    ToggleAddAlarm,
    SetAlarmLabel(String),
    SetAlarmHour(u8),
    SetAlarmMinute(u8),
    SetAlarmWeekday(u8),
    SetAlarmOccurrence(AlarmOccurrence),
    SetAlarmTune(String),
    SetAlarmActive(bool),
    SetAlarmCloseTask(bool),
    ToggleAlarmDevice(String),
    SubmitAddAlarm,
    CancelAddAlarm,
    OpenTimePicker,
    SubmitTimePicker(Time),
    CancelTimePicker,
    OpenDatePicker,
    SubmitDatePicker(Date),
    CancelDatePicker,
    FetchAlarmTunes,
    AlarmTunesReceived(Vec<String>),
    ToggleAlarmPop,
    TriggerAlarm(String),
    PlayAlarmAudio(String),
    ResetSnooze(String),
    SnoozeAlarm,
    SnoozePressStart,
    SnoozePressEnd,
    DismissAlarm,
    SetTurnOff(bool),
    SetSnoozeMinutes(u32),
    DismissNotification(usize),
    ShowNotification(String, String),
    AlarmHovered(String),
    AlarmUnhovered,
    EditAlarm(String),
    DeleteAlarm(String),
    ToggleAlarmActive(String),
    AlarmAddResult(Result<Option<Alarm>, String>),
    AlarmEditResult(Result<Option<Alarm>, String>),
    AlarmDeleteResult(Result<(), String>),
    AddDevice,
    EditDevice(String),
    SelectWelcomeDevice(DeviceSelect),
    ToggleViewableDevice(String),
    SetEditingDeviceName(String),
    SetEditingDeviceType(DeviceType),
    DeleteDevice(String),
    DeviceAddResult(Result<(), String>),
    RequestDelete(PendingDelete),
    ConfirmDelete,
    CancelDelete,
    SaveDeviceEdit,
    CloseDeviceEdit,
    WindowIdReceived(Option<window::Id>),
    CloseRequested(window::Id),
    WindowClosed(window::Id),
    TrayShowWindow,
    TrayToggle,
    TrayQuit,
    ToggleUserMenu,
    ToggleDevicesModal,
    ToggleEditProfile,
    ToggleAbout,
    RefreshSession,
    LogoHovered,
    LogoUnhovered,
    SetEditScreenName(String),
    SetEditFirstName(String),
    SetEditLastName(String),
    SetEditEmail(String),
    SetEditPassword(String),
    SetEditNewPassword(String),
    SetEditConfirmPassword(String),
    ToggleEditChangePassword,
    SubmitEditProfile,
    CancelEditProfile,
    StopwatchStart,
    StopwatchStop,
    StopwatchReset,
    StopwatchLap,
    CountdownStart,
    CountdownStop,
    CountdownReset,
    CountdownSetHours(u8),
    CountdownSetMinutes(u8),
    CountdownSetSeconds(u8),
    FetchTimers,
    TimersReceived(Vec<crate::state::Timer>),
    SaveTimer(String),
    LoadTimer(String),
    TimerSaveResult(Result<(), String>),
    TimerDeleteResult(Result<(), String>),
    ToggleSavedTimers,
    ExportTimerCsv(String, bool),
}
